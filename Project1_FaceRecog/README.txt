digestImg.py



Args: image repository name [,database name]

What it does: takes each image from the repository and transforms them into a vector which it then stores in the database

Note: image repository needs to be a folder inside of your cwd.
